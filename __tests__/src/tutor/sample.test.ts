import { describe, expect, it } from 'vitest';

describe( "sample", () => {
  it( "should pass", () => {
    expect( 1 ).toBe( 1 )
  } )
  it( "should pass", () => {
    expect( 2 ).toBe( 2 )
  } )
} )